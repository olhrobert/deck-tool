---
id: paragraph-title
kind: component
use_when:
  - A heading inside slide content (not the slide chrome headline)
not_when:
  - The slide’s main title (use slide-title)
  - Cover titles (use `<text family="cover-title">`)
---

# Paragraph title

`<paragraph-title size="sm|md|lg" tone="strong" context="slide|surface">`. Sizes map to `components.paragraphTitle.size*`.

Canonical markup: `design-system/components/typography/paragraph-title-md.html`.
