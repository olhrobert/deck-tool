---
id: app-switch
kind: showcase
use_when:
  - Showcase Light/Dark and brand toggles
not_when:
  - Slides. Slides set `color-theme` on `<slide>`; they do not include this control
---

# App switch

Segmented control used only by the showcase. Keep `color-theme="light"` on the switch so it stays readable on either canvas.

Canonical markup: `design-system/showcase/app-switch.html`.
